groups:
  - name: "Networking"
    description: "Networking configuration for DHCP server"

questions:
  - variable: network
    label: "Network for DHCP server"
    description: "Configure the network for DHCP server"
    group: "Networking"
    schema:
      type: string
      required: true
      $ref:
        - "definitions/interface"