autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80

image:
  repository: nginx
  pullPolicy: Always

podAnnotations: {
  "Release": "Stable",
  "Environment": "Test",
  "Tier": "Backend",
  "Service": "HTTP",
  "App": "NGINX"
}

replicaCount: 1

resources:
  limits:
    ephemeral-storage: 64M
    memory: 64M
  requests:
    ephemeral-storage: 32M
    memory: 32M

service:
  port: 80
  protocol: TCP
  type: NodePort